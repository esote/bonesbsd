Patches are not generated specifically for the BonesBSD kernel. After installing
the BonesBSD kernel, using syspatch(8) should be sufficient.

Changes made to the BonesBSD kernel do sometimes require changes be made to the
OpenBSD userspace. Use these patch files if you encounter userspace errors.

0001-Remove-rc-NFS-reference.patch
	Notes: Apply to /etc/rc.
